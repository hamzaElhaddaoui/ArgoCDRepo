apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: inli-pvc
  namespace: inli-ns
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi